import SwiftUI
import Domain
import Core

// UI Package
// SwiftUI views, components, and theming

public enum UI {
    public static let version = "1.0.0"
}

// Re-export all public UI components
// Components are defined in their respective files:
// - Theme.swift: Design system colors, spacing, corner radius
// - UsageProgressBar.swift: Progress bar with utilization display
// - BurnRateBadge.swift: Burn rate level indicator badge
// - SettingsComponents.swift: SectionHeader, SettingsToggle
